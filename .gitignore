# Local machine config — never commit
.local-repos

# Local infrastructure config — never commit (contains real credentials/IDs)
sre-config.md

# Runtime output — generated by skills, stays local
output/
!output/**/.gitkeep

# macOS
.DS_Store

# Build artifacts
sre-skill-*.zip

# Pack script output directory
sre-skill/
